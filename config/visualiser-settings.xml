<settings>

    <!-- real time: "auto"
         load data: any other value -->
    <mode>load</mode>

    <!-- compact: "min"
         full window: any other value -->
    <layout></layout>

    <!-- TODO -->
    <session id="3" channels="1, 2, 5" />

</settings>